#include <iostream>
int main(){
	std::cout << "Read each file." << std::endl: //error for ":"
	
	std::cout << Update master. << std::endl; //error missing "" for string.

	std::cout << "Write new master" std::endl; //error for missing <<.

	int v1 =0,v2=0;
	std::cin >> v >> v2;  //error declar for v,shoube v1

	cout << v1 + v2 << std::endl; // error for delcar cout ,should be
								  //std::cout
	return 0;
}
